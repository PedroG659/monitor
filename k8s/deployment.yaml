apiVersion: apps/v1
kind: Deployment
metadata:
  name: monitor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: monitor
  template:
    metadata:
      labels:
        app: monitor
    spec:
      containers:
        - name: monitor
          # Imagem hospedada no Docker Hub
          image: tymsohi/monitor:latest
          # Boa prática para evitar falhas caso a imagem já exista localmente
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000